# =============================================================================
# CosplayVault — Environment Variables Template
# =============================================================================
# On your VPS: cp env.example.txt .env  (in the project root)
# Then fill in all values before running the app.
# NEVER commit .env to version control.
# =============================================================================

# ─── Application ─────────────────────────────────────────────────────────────

NODE_ENV=production
PORT=3000
# Loopback only in production — nginx terminates TLS on 443
HOST=127.0.0.1

# ─── Database (MySQL 8.x) ────────────────────────────────────────────────────
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Local MySQL: mysql://cosplayvault:STRONG_PASSWORD@127.0.0.1:3306/cosplayvault
DATABASE_URL=mysql://cosplayvault:STRONG_PASSWORD@127.0.0.1:3306/cosplayvault

# ─── Authentication ───────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
JWT_SECRET=REPLACE_WITH_32_CHAR_RANDOM_SECRET

# Manus OAuth (leave empty to use local email/password auth only)
VITE_APP_ID=
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://manus.im
OWNER_OPEN_ID=
OWNER_NAME=Admin

# ─── Wasabi S3 Storage ────────────────────────────────────────────────────────
WASABI_REGION=us-east-1
WASABI_BUCKET=cosplayvault-media
WASABI_ACCESS_KEY_ID=YOUR_WASABI_ACCESS_KEY
WASABI_SECRET_ACCESS_KEY=YOUR_WASABI_SECRET_KEY
WASABI_ENDPOINT=https://s3.us-east-1.wasabisys.com
# Optional CDN prefix (e.g. https://cdn.yourdomain.com)
CDN_BASE_URL=

# ─── Stripe Payments ─────────────────────────────────────────────────────────
# Use sk_test_... / pk_test_... for testing, sk_live_... / pk_live_... for production
STRIPE_SECRET_KEY=sk_test_YOUR_KEY
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_KEY
# Webhook secret from: Stripe Dashboard → Developers → Webhooks → your endpoint
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET

# ─── Email (SMTP) ─────────────────────────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=noreply@yourdomain.com
SMTP_FROM_NAME=CosplayVault

# ─── Manus Built-in APIs (only when hosted on Manus platform) ────────────────
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
VITE_APP_TITLE=CosplayVault
VITE_APP_LOGO=
